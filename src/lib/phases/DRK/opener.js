import { ACTION_TYPES } from '../../IDConstants'
// Opener Name
const opener = []

//  opener.push({ name: 'Soulsow', type: ACTION_TYPES.GCD, note: 'whenever' })
//  opener.push({ name: 'Arcane Circle', type: ACTION_TYPES.oGCD })

//  Actions before the pull

//  Pull
opener.push({ name: 'Pull', type: ACTION_TYPES.PULL })

//  Actions after the pull

export default opener
